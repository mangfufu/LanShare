# LanShare 交接文档 v3.2.0

## 当前状态

项目正常运行，NSFW（海外剧）功能已完成适配。以下问题待解决。

## 待解决问题

### 1. 表格列字体不统一
- **现象**：上传时间、操作列的字号/粗细与其他列（大小、类型、修改时间）看起来不一致
- **位置**：`dev/static/styles.css` 第853-861行
- **提示**：CSS 规则已把 `.size-cell/.time-cell/.born-cell/.type-cell` 放在同一组，但视觉仍有差异。可能是操作列的按钮（`.op-cell .link-button`）高度/行高与纯文本 td 不一致导致。

### 2. 状态下拉框高度不统一  
- **现象**：待开始/进行中/已完成按钮与其他操作按钮高度不一致
- **位置**：`dev/static/styles.css` `op-cell .link-button` 等规则
- **已有改动**：已加 `min-height:30px; display:inline-flex; align-items:center`

## 版本管理
- dev 端口：8082（标题含"测试版"）
- 正式版端口：8080
- 同步需经用户确认
- 同步命令见 CLAUDE.md

## 近期功能（v3.2.0）
- NSFW 海外剧入口：左上角密码输入，验证后复用主页面功能，指向 shared_NSFW 目录
- 本机改密码（点击"密"按钮）
- 上传时间列（新增）
- 项目状态系统（待开始/进行中/已完成）
- 创建项目视频目录格式：第01集补充镜头/粗剪素材/未使用素材
- 文件夹大小缓存隔离
- 点文件（.开头）隐藏
